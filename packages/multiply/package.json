{
  "name": "multiply",
  "version": "1.0.0",
  "main": "index.js",
  "devDependencies": {},
  "scripts": {
    "test": "node --test",
    "publish": "npm publish"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
